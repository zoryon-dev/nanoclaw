# Optional local overrides. Production credentials should live in the OneCLI vault
# (onecli vault set <KEY>=<VALUE>) — the container proxy injects them at request time.

# Composio (Tool Router via @composio/core)
# Account-level API key from app.composio.dev (prefix: ak_).
# Prefer: onecli vault set COMPOSIO_API_KEY=ak_...
# COMPOSIO_API_KEY=
